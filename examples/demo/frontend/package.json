{
  "name": "inertia-gleam-frontend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build:css": "npx @tailwindcss/cli -i ./src/styles.css -o ../static/css/styles.css --minify",
    "watch:css": "npx @tailwindcss/cli -i ./src/styles.css -o ../static/css/styles.css --watch",
    "build:js": "esbuild src/main.tsx --bundle --outdir=../static/js --format=esm --splitting --chunk-names=[name]-[hash] --jsx=automatic",
    "build:ssr": "esbuild src/ssr.tsx --bundle --outdir=../ssr --format=cjs --platform=node --target=node18 --jsx=automatic",
    "watch:js": "esbuild src/main.tsx --bundle --outdir=../static/js --format=esm --splitting --chunk-names=[name]-[hash] --watch --jsx=automatic",
    "watch:ssr": "esbuild src/ssr.tsx --bundle --outdir=../ssr --format=cjs --platform=node --target=node18 --watch --jsx=automatic",
    "build": "npm run type-check && npm run build:css && npm run build:js && npm run build:ssr",
    "build:fast": "npm run build:css && npm run build:js && npm run build:ssr",
    "watch": "npm run build:css && npm run build:ssr && npm-run-all --parallel watch:css watch:js watch:ssr",
    "dev": "npm run watch",
    "dev:quick": "npm run build:fast && npm-run-all --parallel watch:css watch:js watch:ssr",
    "type-check": "tsc --noEmit",
    "type-check:watch": "tsc --noEmit --watch",
    "validate-types": "npm run type-check && echo '✅ All types are valid'",
    "dev:safe": "npm run type-check && npm run watch",
    "build:ssr-only": "npm run build:ssr"
  },
  "dependencies": {
    "@headlessui/react": "^2.2.4",
    "@inertiajs/react": "^1.0.14",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.7",
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "esbuild": "^0.19.11",
    "npm-run-all": "^4.1.5",
    "tailwindcss": "^4.1.7",
    "typescript": "^5.0.0"
  }
}

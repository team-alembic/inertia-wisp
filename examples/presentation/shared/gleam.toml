name = "shared"
version = "1.0.0"
description = "Shared encoders for backend and frontend property-based testing"
target = "erlang"

[dependencies]
gleam_stdlib = ">= 0.60.0"
gleam_json = ">= 3.0.0"

[dev-dependencies]
gleeunit = ">= 1.0.0"

# JavaScript specific configuration (optional)
[javascript]
# Generate TypeScript .d.ts files
typescript_declarations = true

name = "presentation_shared"
version = "1.0.0"

# Package links
licences = ["MIT"]
description = "Shared types for presentation example - compiles to both Erlang and JavaScript"
repository = { type = "github", user = "mbuhot", repo = "inertia-wisp" }

# No target specified - will be built with --target flag
# gleam build --target javascript (for frontend)
# gleam build --target erlang (for backend)


[javascript]
typescript_declarations = true


[dependencies]
gleam_stdlib = ">= 0.34.0 and < 2.0.0"
gleam_json = ">= 3.0.0 and < 4.0.0"
